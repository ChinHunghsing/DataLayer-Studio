interface:
  display_name: "DataLayer Studio 时间线"
  short_description: "维护 DataLayer Studio 时间线模型、编辑状态、工程兼容和预览一致性"
  default_prompt: "Use $dls-timeline to change DataLayer Studio timeline behavior without breaking saved projects."
