# DataLayer Studio 界面与操控重构方案（0.3 「Studio Reset」）

- 制定日期：2026-07-12
- 基线：0.2.2（时间线里程碑已收口）+ 0.2.3 特性 A（自动对表）已完成
- 性质：**从零评审**的产品/UX 重构方案，不受既往整改队列约束；允许打破现有用户习惯
- 范围：macOS 端 `Sources/OverlayStudio`；共享层改动需保证移动端不受影响

---

## 1. 评审结论：现状诊断（按严重度排序)

以下问题基于当前代码逐文件核对，附定位。

### S1 · 严重：范式撕裂 —— 向导隐喻叠在 NLE 骨架上

- `SidebarView.swift:14-33`：左栏仍是「步骤 1 素材 → 步骤 2 画布」的向导编号。这是单素材时代的遗产；0.2.2 已经是多轨时间线编辑器，用户操作根本不是线性的，编号只制造「我必须按顺序做」的错误心智。
- `StudioModel` 仍保留「活跃视频素材 / 活跃活动素材」（`activeVideoAssetID`）概念，与时间线「片段才是事实」的权威模型并存。素材池里一份素材同时有「选中 / 活跃 / 在时间线使用中」三种状态，用户无法区分。
- 结论：**左栏的定位必须重写**——它应该是「库」（素材、组件、模板），不是「向导 + 杂项设置」。

### S2 · 严重：22+ 种数据组件没有可见入口

- 添加 gauge 的唯一入口是检查器头部的 `+` 下拉菜单（`InspectorSelectionHeader.swift:114`），一个纯文字长列表。
- 这是本产品的**核心资产**（22 种遥测组件 + 天气/路线/时间），却被藏在三级深的菜单里；新用户完全无法浏览「我能加什么」。竞品（Telemetry Overlay、Garmin 官方）都把组件库做成可视化图库。
- 结论：组件库必须成为一等公民面板，带渲染预览、搜索、拖放上画布。

### S3 · 高：检查器过载，编辑路径太深

- `InspectorSettingsPanel.swift` 2016 行，五分段（布局/内容/外观/字体/数据）× 折叠组 × 「标签/数值/单位/图标」四部件矩阵（`InspectorSettingsPanel.swift:728-790` 的巨型 case 表）。
- 改一个数值字号的路径：选中元素 → 切「字体」分段 → 展开折叠组 → 在四部件 Picker 里选「数值」→ 调字号。四步深，且「部件」这个抽象概念用户在画布上看不见。
- 位置/大小用滑杆 + 裸数值混排（X=201、Y=17.901 单位制不一致），精调体验差。
- 结论：检查器需要「常用恒显 + 高级渐进披露」重构，部件选择应搬到画布上（点选即编辑）。

### S4 · 高：双时间轴冗余

- 预览控制条里有一条 `PreviewTimelineSlider`（限制在导出区间内），底部又有权威时间线 `ProjectTimelineView` 的播放头；两条时间轴显示同一播放位置，导出范围也有两套控制（`ExportTrimRangeControl` + 时间线 `exportRangeLayer`，`ProjectTimelineView.swift:838`）。
- 结论：**只留一条时间轴**。预览控制条只保留 transport（播放/逐帧/时间码）；导出范围统一为时间线 In/Out 点（I/O 快捷键，行业标准）。

### S5 · 高：启动即模态审问

- 空项目启动弹 `StartupSourcePromptView` 模态（`ContentView.swift:434`），只问「选视频还是选活动」。0.2.3-B 已做了最近工程菜单，但入口在菜单栏，首屏没有承接。
- 结论：改为**欢迎窗口**：最近工程网格（缩略图）+ 新建工程 + 内置模板 + 整窗拖放导入 + 示例工程。这是新用户「第一次打开就懂」的关键一屏。

### S6 · 中：手动网格配置是伪需求

- 左栏让用户配置 21×64 的行列数网格再开吸附（`SidebarView.swift:122-146`）。没有用户想「设定 30 行网格」；他们想要的是「拖动时自动对齐别的元素和安全边距」。
- 结论：删除行列配置，改为 Keynote 式**智能参考线**（元素边缘/中心互相吸附、画布中线、可配置安全框），网格仅保留开关。

### S7 · 中：导出体验分散

- 入口在标题栏 accessory 按钮（`OutputToolbarAccessory`）、设置在弹出面板（`OutputPanelView` 的 picture/encoding 卡片）、进度在另一个 sheet（`ExportStatusSheet`）、裁剪又在时间线。
- 分辨率/帧率/码率/编码 每次导出都要重复确认，没有「导出预设」概念。
- 结论：统一为**导出中心**（⌘E 唤起单一 sheet）：左侧命名预设（透明浮层·ProRes / YouTube 4K / 竖屏社媒 / 自定义…），右侧摘要与覆盖项，进度就地显示。

### S8 · 中：状态通信原始

- 全局状态是左栏页脚一行 `model.status` 文本（`SidebarView.swift:46`），瞬时消息（「已保存」）与持久状态（「运动开始已设置在 50 秒」）混在同一条里互相覆盖。0.2.3-D 已承认此债（toast 通道）。
- 结论：瞬时消息走 toast（自动消失、可堆叠），持久状态就地显示（同步状态放时间线片段角标、导出状态放导出中心），删除页脚状态行。

### S9 · 中：面板刚性

- 左 330pt / 右 390pt 固定宽度不可调、不可折叠（`ContentView.swift:145-163`）；最小窗口 1320×760。全屏预览是独立模式切换而非面板收纳。
- 结论：三面板可拖拽调宽、可一键折叠（⌥⌘1 库 / ⌥⌘2 时间线 / ⌥⌘3 检查器，对齐 FCP 习惯）；「全屏预览」自然等价于「折叠全部面板」，不再是独立模式。

### S10 · 低：预设（模板）是文字列表

- 布局预设是「文本框起名 + 图标按钮保存 + 文字行列表」（`SidebarView.swift:148-205`），无缩略图、无内置模板。对「快速出片」这个核心卖点来说，模板应该是可视化的。

---

## 2. 设计原则（重构北极星）

1. **一条主线**：新用户的完整旅程是「拖进来 → 自动对表 → 挑个模板 → 导出」，四步全部在首屏可见，零菜单探索。
2. **画布即编辑器**：能在画布上直接完成的（选部件、对齐、微调），不进检查器；检查器只做画布表达不了的属性。
3. **一个事实源**：时间线是时间的唯一事实源；素材池只是仓库；不再有「活跃素材」。
4. **库在左、属性在右、时间在下**：与 FCP/DaVinci/Motion 一致的空间记忆，不发明新布局。
5. **默认智能，手动兜底**：自动对表、智能参考线、导出预设是默认路径；手动偏移、网格、自定义参数保留为高级路径。

---

## 3. 新信息架构

```
┌────────────┬──────────────────────────────┬─────────────┐
│ 库 Library │          预览画布              │ 检查器       │
│ (可折叠)    │   on-canvas 直接操控           │ (可折叠)     │
│ ┌────────┐ │   智能参考线 / 部件点选         │ 快速控件恒显  │
│ │素材|组件│ │                              │ ──────────  │
│ │  |模板 │ │   transport：⏯ 逐帧 时间码     │ 内容        │
│ └────────┘ │  （不再有第二条时间轴）          │ 样式        │
│ 拖放添加    │                              │ 数据        │
├────────────┴──────────────────────────────┴─────────────┤
│ 时间线（唯一时间轴）：多轨 + 对表标记 + In/Out 导出范围        │
└──────────────────────────────────────────────────────────┘
顶部工具栏(NSToolbar)：面板开关 ⌥⌘1/2/3 · 自动对表 · 导出 ⌘E
独立：欢迎窗口（最近工程/模板/示例）· 导出中心（预设+摘要+进度）
```

关键变化对照：

| 现在 | 之后 |
| --- | --- |
| 左栏「步骤1素材/步骤2画布」 | 左栏「库」三 tab：素材池 / 组件库(可视化) / 模板库(缩略图) |
| 检查器 `+` 菜单加组件 | 组件库拖放/双击上画布 |
| 五分段检查器 + 部件 Picker | 快速控件恒显 + 内容/样式/数据三段；部件在画布点选 |
| 预览时间轴 + 时间线双轴 | 只留时间线；transport 只有播放控制 |
| 手动 21×64 网格 + 吸附开关 | 智能参考线 + 安全框；网格只剩显示开关 |
| 标题栏输出按钮 + 多个 sheet | 导出中心（⌘E，预设制） |
| 启动模态「选视频/选活动」 | 欢迎窗口（最近工程/模板/拖放/示例） |
| 页脚 status 一行字 | toast + 就地状态 |
| 固定 330/390 面板 | 可调宽、可折叠面板；全屏=全折叠 |

---

## 4. 分模块详细方案

### 4.1 欢迎窗口（新增 `WelcomeWindowView`）

- 独立 window scene：左侧「新建工程 / 打开工程 / 示例工程」，右侧最近工程网格（工程缩略图 = 保存时渲染一帧画布 PNG 存入工程包）。
- 底部横排内置模板（比赛/训练/极简，吸收 0.2.3-D 顺延项），点击即「新建工程 + 应用模板」。
- 整窗接受拖放：视频/FIT/GPX/工程文件，拖入即建工程并自动对表。
- 有工程打开时不显示；「窗口 ▸ 欢迎窗口」可召回。删除 `StartupSourcePromptView` 与两个 source-continuation alert（`ContentView.swift:402-432`）——自动对表已让「先选哪个」失去意义。

### 4.2 库面板（重写 `SidebarView` → `LibraryPanelView`）

- 顶部分段：**素材 · 组件 · 模板**。
- 素材：现 `MediaPoolList` 保留（在用/离线/relink 状态已完善），删除「活跃」概念——预览跟随时间线播放头，不跟随素材选中；点选素材只做高亮与检查器显示元信息。
- 组件：网格图库，每格是该 gauge 用示例数据实时渲染的缩略图（复用 `OverlayCore` 渲染器，缓存位图）；按「指标块 / 仪表盘 / 地图·进度 / 信息块」分组；顶部搜索框；拖到画布落点即添加，双击加到默认位置。
- 模板：布局预设升级为缩略图卡片（应用预设到示例画布渲染小图）；「把当前布局存为模板」按钮取代文本框+图标；内置三套模板与用户模板同列，内置的带徽标不可删。iCloud 同步状态挪到模板 tab 页脚一行。
- 网格行列 stepper、吸附开关从左栏删除（见 4.3）。

### 4.3 画布操控（改 `PreviewCanvasView`）

- **智能参考线**：拖动元素时对齐其他元素边缘/中心、画布中线/三分线、安全框（内缩百分比可在设置中调），吸附时显示黄色参考线并轻微磁吸。替换现有网格吸附；`showGrid` 保留为纯视觉开关（视图菜单）。
- **部件子选择**:单击选元素，再单击选中元素内部件（标签/数值/单位/图标），画布上部件高亮，检查器自动切到该部件的字体/颜色。Esc 逐级退出。检查器里的部件 Picker 矩阵随之删除。
- 多选（⇧点选/框选）+ 对齐分布命令（左/右/顶/底/水平居中/垂直居中/等距），进「排列」菜单与右键菜单。
- 右键菜单补全：复制样式/粘贴样式、置于顶层/底层、删除。
- transport 条精简：播放/暂停、逐帧、时间码输入（`TimecodeField` 保留）、缩放/适配控件；删除 `PreviewTimelineSlider`。

### 4.4 检查器重构（重写 `InspectorSettingsPanel` 的组织层）

- **快速控件区**（恒显，不折叠）：X/Y/宽/高数值字段（统一像素单位、可拖拽划动改值）、不透明度、显示/隐藏。删除滑杆+裸数值混排。
- 分段从五个减为三个：**内容**（该组件显示什么：标签文案、单位制、图标选择）、**样式**（面板底色/圆角/描边、字体——作用于当前画布选中的部件）、**数据**（精度、仪表 min/max、天气 key、平滑）。原「布局」分段被快速控件区吸收，「外观+字体」合并为「样式」（随画布部件选择联动）。
- 折叠状态记忆保留（`@SceneStorage` 机制沿用）。
- 时间线片段检查器（`TimelineClipInspectorView`）保持现结构，仅套用新视觉令牌。

### 4.5 时间线与对表（改 `ProjectTimelineView` + `BottomWorkspaceView`）

- 唯一时间轴：导出范围 = In/Out 点（快捷键 I/O 设置、⌥X 清除），替换 `ExportTrimRangeControl`；范围高亮样式沿用 `exportRangeLayer`。
- **对表可视化**：自动对表完成后，在视频片段与活动片段之间画对齐标记（同刻竖线 + 「已按拍摄时间对表」角标）；拖动活动片段 = 手动微调偏移，按住 ⌥ 出现 ±毫秒微调字段。原「同步模式」三选一收进片段检查器的「高级对表」折叠组，标记运动开始 ⌘⇧S 保留。
- 头部工具行精简：轨道操作收进右键菜单；保留缩放控件与「自动对表」按钮（对整个时间线重跑）。

### 4.6 工具栏与窗口（改 `StudioWindowView`）

- 用真正的 NSToolbar（`.toolbar` API）替换 `TitlebarTrailingAccessory` 私有注入：左侧面板开关三连（库/时间线/检查器，⌥⌘1/2/3），中间标题（工程名·未保存点），右侧「自动对表」「导出 ⌘E」。
- 三栏改 `HSplitView`（或自绘可拖分栏），左 260–420、右 320–480 可调，可折叠；`bottomResizeHandle` 机制保留。折叠全部 = 现「全屏预览」，删除独立 fullscreen 模式分支（`ContentView.swift:22-27`）。
- 最小窗口降到 1100×700（面板可折叠后成立）。

### 4.7 导出中心（重写 `OutputPanelView` + `ExportStatusSheet` 合一）

- ⌘E 唤起单一 sheet：左列**导出预设**（内置：透明浮层·ProRes 4444 / 透明浮层·HEVC / 成片·4K HEVC / 成片·1080p H.264 / 竖屏 4K；用户可另存自定义预设），右侧当前预设的参数与覆盖项 + 摘要（沿用 `ExportSummaryCard` 数据）+ 目标位置。
- 点导出后 sheet 就地切换为进度视图（进度/ETA/取消沿用现逻辑），完成态就地显示结果与「访达中显示/打开」。三个 sheet 合为一个。
- 预设为新增 Codable 存储，走 `StudioPreferenceStore`，向后兼容零风险（新文件）。CLI 等效命令提示（0.2.3-C6）放在摘要下方一行可复制文本。

### 4.8 状态系统

- 新增 toast 层（画布右下角，自动 3s 消失，可堆叠 3 条）：保存成功、预设应用、自动对表结果、复制完成等瞬时消息。
- 持久状态就地化:同步状态→时间线片段角标；离线素材→素材池行（已有）；导出→导出中心。删除左栏页脚 `statusFooter`。
- `model.status` 字符串通道保留为 debug console 日志源，不再上屏。

### 4.9 视觉令牌收敛

- `ShellStyle` 扩展为完整令牌集：三级表面（面板/分组/控件）、两级描边、四级字号（11/12/13/15）、统一 8pt 间距栅格、统一圆角（6/8/10）。
- 全局 `controlSize(.small)` 改为分层：快速控件区 .regular，高级折叠区 .small。
- 数字字段统一组件（现 `NumberField`/`InlineIntField`/`CompactNumberIntField` 三套合一），支持拖拽划动改值；禁千分位逗号（既有约定）。

---

## 5. 实施路线（三个版本，每步独立可发布）

### 0.3.0 「骨架重置」——改布局不改功能语义

1. 欢迎窗口（4.1）＋删除启动模态。
2. 库面板三 tab（4.2）：组件库图库（先用静态渲染缩略图）、模板缩略图、素材池去「活跃」。
3. NSToolbar + 可调/可折叠面板 + 删除独立全屏模式（4.6）。
4. 时间线唯一化：删 `PreviewTimelineSlider`，In/Out 替换裁剪控件（4.5 前半）。
- 验收：`OverlayStudioTests` 全绿；`scripts/build_app_bundle.sh`；四语言新文案齐；旧工程/旧 preset 加载不回退；导出产物逐帧与 0.2.3 对比一致（布局改动不得影响渲染）。

### 0.3.1 「操控重置」——画布与检查器

5. 智能参考线替换网格吸附（4.3），删除行列配置。
6. 画布部件子选择 + 检查器三段化 + 快速控件区（4.3/4.4）。
7. 多选与对齐分布命令。
- 验收：新增画布交互单测（吸附计算、子选择状态机）；检查器每个组件类型人工过一遍四语言。

### 0.3.2 「出口重置」——导出与反馈

8. 导出中心 + 导出预设（4.7），合并三 sheet。
9. toast 状态系统，删页脚状态行（4.8）。
10. 对表可视化标记与 ⌥ 微调（4.5 后半）。
11. 视觉令牌收敛扫尾（4.9）。
- 验收：导出全矩阵（浮层×2 编码 + 成片×2 编码 × In/Out）回归；透明边缘 QA 重跑（红线）。

### 明确不做（本重构范围外）

- 不动 `OverlayCore` 渲染/写出管线；不动透明 Alpha 路径；不动购买/签名/上架逻辑。
- 不做多窗口多工程、插件系统、时间线转场/音频轨（交接文档既定边界）。
- 移动端同步改版另行排期；本方案共享层改动仅限 `OverlayStudioKit` 可平移的令牌与模型。

---

## 6. 风险与守护

| 风险 | 守护措施 |
| --- | --- |
| 渲染回归（布局重构误伤画布状态） | 每阶段用 `assets/resourses/` 素材做导出逐帧对比；`OverlayRendererTests` 全跑 |
| 旧 preset/工程兼容 | 布局模型零改动优先；新增字段全部 optional + 默认值；schema 1/2 工程加载测试 |
| 快捷键冲突 | I/O/⌥⌘1-3 新增前全量排查 `Commands` 定义 |
| 打破老用户习惯 | 0.3.0 发布说明附「界面搬家地图」；菜单命令名保持稳定，菜单是老用户的兜底路径 |
| 工程量失控 | 三版本各自独立可发布；任何一版可单独上 TestFlight 验证 |

## 7. 成功指标

- 新用户路径：从空状态到导出成片 ≤ 4 次主要交互（拖入素材 → 确认自动对表 → 选模板 → ⌘E 导出）。
- 组件发现：22+ 组件全部可视化可浏览，添加任一组件 ≤ 2 次点击/一次拖放。
- 属性编辑深度：改「数值字号」从 4 步降到 2 步（画布点数值 → 检查器改字号）。
- 时间轴唯一：界面上表示时间的控件只剩时间线一处。
